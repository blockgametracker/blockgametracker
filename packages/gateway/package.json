{
    "name": "@repo/gateway",
    "version": "0.0.0",
    "private": true,
    "main": "./dist/index.js",
    "type": "commonjs",
    "types": "./src/index.ts",
    "scripts": {
        "build": "rm -rf dist && tsc",
        "dev": "rm -rf dist && tsc --watch"
    },
    "devDependencies": {
        "typescript": "^5.1.3"
    }
}
