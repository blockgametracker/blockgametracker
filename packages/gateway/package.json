{
    "name": "@repo/gateway",
    "version": "0.0.0",
    "private": true,
    "main": "./dist/index.js",
    "type": "module",
    "types": "./src/index.ts",
    "scripts": {
        "build": "rimraf dist && tsc",
        "dev": "rimraf dist && tsc --watch"
    },
    "devDependencies": {
        "rimraf": "^5.0.7",
        "typescript": "^5.4.5"
    },
    "dependencies": {
        "server-only": "^0.0.1"
    }
}
